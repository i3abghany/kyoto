cdecl fn printf(fmt: str, ...) i32;
cdecl fn exit(code: i32) void;

import vec;

fn main() i32 {
    var ve: vec::Vector<str>* = new vec::Vector<str>(2);
    ve.push("hello");
    ve.push("world");
    ve.push("!");
    printf("%s %s%s\n", ve.get(0), ve.get(1), ve.get(2));
    return 0;
}